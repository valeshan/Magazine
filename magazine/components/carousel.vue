<template>
  <div>
    <b-carousel
      id="carousel-1"
      v-model="slide"
      :interval="4000"
      indicators
      background="#ababab"
      img-width="1024"
      img-height="600"
      style="text-shadow: 1px 1px 2px #333;"
      @sliding-start="onSlideStart"
      @sliding-end="onSlideEnd"
    >
      <!-- Text slides with image -->
      <b-carousel-slide :img-src="gallImage"
      ></b-carousel-slide>

      <!-- Slides with custom text -->
      <b-carousel-slide :img-src="gallImage">
      </b-carousel-slide>

      <!-- Slides with image only -->
      <b-carousel-slide :img-src="gallImage"></b-carousel-slide>

    </b-carousel>
  </div>
</template>

<script>
import gallImage from '~/assets/images/gallimage.jpeg';

  export default {
    data() {
      return {
        slide: 0,
        sliding: null,
        gallImage
      }
    },
    methods: {
      onSlideStart(slide) {
        this.sliding = true
      },
      onSlideEnd(slide) {
        this.sliding = false
      }
    }
  }
</script>